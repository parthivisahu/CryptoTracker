import React from 'react';

const CryptoGraph = ({ name }) => {
  // Your code to display the graph for the selected cryptocurrency goes here
  return (
    <div>
      <h1>Graph for {name}</h1>
      {/* Add your graph component here */}
    </div>
  );
};

export default CryptoGraph;
